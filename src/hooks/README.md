# /hooks — shared reusable logic

Cross-feature hooks only. Feature-specific hooks live inside their feature folder
(`/features/<feature>/hooks`).

Business logic lives in hooks/services, never in presentational components
(AGENTS.md §8).

Empty for the Phase 1 skeleton.
