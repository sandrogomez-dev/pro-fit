/**
 * Progress feature — PRs, history and charts, auto-derived from workout_logs.
 * Premium-gated (AGENTS.md §3); basic logging stays free.
 */
export { ProgressScreen } from './screens/ProgressScreen';
