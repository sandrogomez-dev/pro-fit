/**
 * Progress feature — the workout history (training log), built from recorded
 * circuit sessions. Premium-gated (AGENTS.md §3); logging stays free.
 */
export { ProgressScreen } from './screens/ProgressScreen';
export { useSessionsSync } from './hooks/useSessionsSync';
