/**
 * Workouts feature — log sets (reps/weight + green done check) per exercise during
 * a workout session. Local-first with background sync.
 */
export { WorkoutScreen } from './screens/WorkoutScreen';
export { useWorkoutSync } from './hooks/useWorkoutSync';
