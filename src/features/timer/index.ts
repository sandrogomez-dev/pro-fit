/**
 * Rest timer feature — a local notification (source of truth, fires when locked)
 * plus a cosmetic on-screen countdown bar. Started when a set is marked done.
 */
export { RestTimerBar } from './components/RestTimerBar';
