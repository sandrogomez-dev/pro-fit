/**
 * AI feature — premium exercise substitution via the deepseek-substitute edge
 * function. Sends only non-personal data; 5/day enforced server-side.
 */
export { AiSubstitute } from './AiSubstitute';
