/**
 * Circuit feature — the guided, hands-free routine runner. Configure a routine's
 * work/rest/rounds, hit Play, and it auto-advances through the exercises.
 */
export { CircuitRunnerScreen } from './screens/CircuitRunnerScreen';
export { CircuitSettings } from './components/CircuitSettings';
export { buildCircuit, DEFAULT_WORK_SECONDS, DEFAULT_CIRCUIT_REST_SECONDS } from './buildCircuit';
